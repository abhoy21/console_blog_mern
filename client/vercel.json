{
  "version": 2,
  "builds": [
    { "src": "*.js", "use": "@vercel/next" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "server/index.js" }
  ],
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
